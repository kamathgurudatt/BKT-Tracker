import 'package:flutter/material.dart';
import '../widgets/sentinel_scaffold.dart';
class WishlistScreen extends StatelessWidget { const WishlistScreen({super.key}); @override Widget build(BuildContext context) => const SentinelScaffold(title: 'Wishlists', child: Center(child: Text('No placeholder inventory is shown. Add products from live search results to populate wishlists.'))); }
